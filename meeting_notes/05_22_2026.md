# Meeting Notes: 05/22/2026

## Attendees

- Faizan Siddiqi
- Nishita Bhakar

## Agenda

- Walk through the full AskTIM website function end-to-end.
- Plan Railway hosting for the live deployment.
- Review the wording of the new AskTIM self-context file.
- Decide how to implement image context for input and output.
- Confirm next steps across the open work.

## Notes

### Deployment

- AskTIM is ready to deploy. Romain just needs to host it through Railway.

### Image context for input and output

- Still need to figure out image context implementation for inputs and outputs.
- Image context implementation can be added after deployment — not a launch blocker.
- The first exercise affected by image context is exercise 4 in week 4, so we have about two weeks of runway before it is needed in front of students.

### AskTIM self-context wording

- The wording of the new AskTIM self-context file looks good. Romain to review it once before launch.
- Concern about giving the tutor double self-context — once through `about_asktim.txt` and again through the existing `tutor_*` system prompt.
- Resolution: add a pointer in `about_asktim.txt` that references `tutor_*` for the actual rules, so the two files don't restate the same constraints.

## Decisions

- Romain will host AskTIM on Railway; deployment is not blocked on image-context work.
- Image context implementation lands after the Railway launch, with the deadline driven by week 4 (exercise 4).
- Romain will do a final wording review of `about_asktim.txt` before launch.
- Add a pointer in `about_asktim.txt` to `tutor_*` for the rules, instead of restating them, to avoid double self-context.

## Action items

- [ ] Romain to host AskTIM on Railway.
- [ ] Romain to review the wording of `about_asktim.txt` before launch.
- [ ] Add a pointer in `about_asktim.txt` referencing `tutor_*` for the rules.
- [ ] Exercise audit and image context:
  - [ ] Add missing image context to relevant exercises.
  - [ ] Create a framework to allow image inputs from students and image input for context.

## Resources

- Current draft of the AskTIM self-context file is `../main_ui/about_asktim.txt`
