# Transcripts

Generated tutor-student conversation transcripts for the Humanities LLM Tutor project.

## Folder Structure

```text
transcripts/
├── chaotic/                         # Chaotic student persona
│   ├── chaotic_raw/                 # 96 raw (ungraded) transcripts
│   ├── chaotic_gpt/                 # 96 GPT-graded transcripts
│   ├── chaotic_claude/              # 96 Claude-graded transcripts
│   ├── chaotic_mini/                # Mini-continuation transcripts (pivot + replay)
│   └── chaotic_two_layer_raw/       # Two-layer tutor raw transcripts (with verifier field)
├── cooperative/                     # Cooperative student persona
│   ├── cooperative_raw/             # 96 raw transcripts
│   ├── cooperative_gpt/             # 96 GPT-graded transcripts
│   ├── cooperative_claude/          # 96 Claude-graded transcripts
│   ├── cooperative_mini/
│   └── cooperative_two_layer_raw/
├── clueless/                        # Clueless student persona
│   ├── clueless_raw/                # 96 raw transcripts
│   ├── clueless_gpt/                # 96 GPT-graded transcripts
│   ├── clueless_claude/             # 96 Claude-graded transcripts
│   ├── clueless_mini/
│   └── clueless_two_layer_raw/
└── README.md
```

## Counts

| Persona     | Raw | GPT-graded | Claude-graded | Total   |
| ----------- | --- | ---------- | ------------- | ------- |
| chaotic     | 96  | 96         | 96            | 288     |
| cooperative | 96  | 96         | 96            | 288     |
| clueless    | 96  | 96         | 96            | 288     |
| **Total**   | 288 | 288        | 288           | **864** |

## Subfolder Convention

Each persona folder uses a suffix pattern to indicate the generation and grading method:

- **`{persona}_raw/`** — Ungraded transcripts generated by the standard tutor+student simulation pipeline. Source-of-truth inputs to the judge.
- **`{persona}_gpt/`** — Copies of raw transcripts with a `grade` object added by the unified judge (`judge/run_judge.py`, provider=`gpt`).
- **`{persona}_claude/`** — Same, graded by the Claude judge (`judge/run_judge.py`, provider=`claude`).
- **`{persona}_mini/`** — Mini-continuation transcripts: a raw transcript forked at a pivot turn, with the student side replayed from file and the tutor regenerated using a new prompt or provider. Includes a `mini_continuation` metadata object.
- **`{persona}_two_layer_raw/`** — Ungraded transcripts generated by the two-layer tutor (standard tutor + rubric-aware verifier, max 1 retry per turn). Each exchange includes a `verifier` field recording whether a retry was requested.

## Transcript JSON Schema

Every transcript file (`transcript_NNNN.json`) follows this structure:

```json
{
  "tutor_prompt": "tutor_03",
  "student_persona": "chaotic_01",
  "course": "philosophy",
  "exercise_number": "01",
  "turn_size": 10,
  "context": "Course description text...",
  "exercise": "Full exercise prompt text...",
  "turns": 10,
  "exchanges": [
    {
      "turn": 1,
      "student": "Student message...",
      "tutor": "Tutor response...",
      "pedagogical_reasoning": "Internal tutor reasoning..."
    }
  ]
}
```

Two-layer raw transcripts additionally contain:

- `verifier_rubric`: rubric name used by the verifier layer (e.g. `rubric_05`)
- Per exchange: `verifier` field — `{"retried": false}` if the first draft was approved, or `{"retried": true, "feedback": "..."}` if the verifier requested a retry

Graded transcripts additionally contain a top-level `grade` object:

```json
{
  "grade": {
    "sections": {
      "1_pedagogy": { "criteria": { "1.1": { ... }, "1.2": { ... }, "1.3": { ... } }, "base": { "score": 22, "max": 24 } },
      "2_dialogue_quality": { "criteria": { "2.1": { ... }, "2.2": { ... } }, "base": { "score": 10, "max": 12 } },
      "3_communication_quality": { "criteria": { "3.1": { ... }, "3.2": { ... } }, "base": { "score": 9, "max": 10 } }
    },
    "max_score": 46,
    "total_base_score": 41,
    "max_base_score": 46,
    "model": { "provider": "openai", "model": "gpt-5.4" },
    "overview": ["Brief evidence-based overview."],
    "total_score": 41,
    "judge_llm_calls": 1
  }
}
```

### Key Fields

| Field | Description |
| ----- | ----------- |
| `tutor_prompt` | Tutor system prompt version used |
| `student_persona` | Student persona identifier (e.g. `chaotic_01`, `cooperative_02`) |
| `course` | Course name: `philosophy` or `urban_studies` |
| `exercise_number` | Exercise identifier within the course |
| `turn_size` | Planned number of exchanges |
| `turns` | Actual number of exchanges |
| `exchanges` | Array of student-tutor exchange objects |
| `grade.total_score` | Total points awarded by the judge |
| `grade.max_score` | Maximum possible score (46 for rubric_05) |

## Student Personas

| Persona | Behavior |
| ------- | -------- |
| **chaotic** | Challenges boundaries, tests edge cases, requests direct answers |
| **cooperative** | Good-student baseline that stays on-task and follows guidance |
| **clueless** | Genuinely confused, needs scaffolding and patience |

Each persona has multiple versions (e.g. `chaotic_01`, `chaotic_02`).

## Transcript Naming

Files are named `transcript_NNNN.json` where `NNNN` is a zero-padded index
(0001–9999). The same index across `_raw`, `_gpt`, and `_claude` folders
corresponds to the same conversation — the graded versions are copies of
the raw file with a `grade` object appended.

## How Transcripts Are Generated

Raw transcripts are produced by the tutor-student simulation pipeline:

1. Select a tutor prompt, student persona, course, and exercise.
2. Run a multi-turn conversation (default 10 exchanges).
3. Save the transcript JSON to `{persona}/{persona}_raw/`.

## How Transcripts Are Graded

```powershell
# Grade all raw transcripts with GPT
python -m ui.run_ui_judge --provider gpt

# Grade all raw transcripts with Claude
python -m ui.run_ui_judge --provider claude
```

Both commands accept `--prompt` and `--rubric` flags. Output goes to the respective
`_gpt` or `_claude` subfolder.

## Visualization

Score comparison charts are generated by the visualization module:

```powershell
python -m visualization.run_visualization
```

Output: `visualization/outputs/individual_grades_gpt_vs_claude.png`
